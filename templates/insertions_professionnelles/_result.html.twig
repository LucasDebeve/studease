<div class="col">
    <div class="rounded-2 d-flex flex-column p-3 h-100 mb-2"
         style="border: 2px solid {{ insertion.typePro == 1 ? '#00ac8c' : '#ac008c' }};">
        <a href="{{ path('app_detail_insertions_professionnelles', {id: insertion.id }) }}" class="d-flex flex-column">
            <img class="rounded-2" src="{{ asset('img/default_profile.jpg') }}"
                 alt="Logo de l'entreprise {{ insertion.company.name }}">
        </a>
        <h4>{{ insertion.titre }}</h4>
        <p class="flex-grow-1">{{ insertion.company.name }}</p>
        <p>{{ insertion.duree }} jours ouvrés</p>
    </div>
</div>